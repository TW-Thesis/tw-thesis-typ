#set heading(numbering: "A.1")
#counter(heading).update(0)

#import "/helper/mod.typ": *

= 編譯問題排查 <appendix-a>

使用模板時，若遇到編譯失敗、圖片找不到或 PDF 沒有更新，可以先依本附錄的順序檢查。建議保留終端機中的錯誤訊息，從第一個錯誤指出的檔案與位置開始處理，修正後再重新編譯。

這份檔案也示範附錄的排版方式。換成自己的論文時，可以保留開頭的編號設定，將標題與內文替換成問卷、補充數據或其他資料，再到 `main.typ` 確認附錄的裝訂順序。

== 指令執行失敗

若終端機顯示找不到 `typst`，請先確認已完成安裝，再重新開啟終端機，執行

```bash
typst --version
```

確認能夠顯示版本資訊後，再嘗試編譯。若找不到的是 `just`，則可以先使用以下指令，直接產生 PDF：

```bash
typst compile main.typ --font-path fonts/
```

若錯誤訊息指出找不到 `main.typ`，請檢查終端機目前所在的資料夾。編譯指令應在專案根目錄執行，也就是能看到 `main.typ`、`config.yml` 與 `contents/` 的位置。若目前位於章節資料夾，請先切換回根目錄再試一次。

== 修改設定後無法編譯

修改設定後出現錯誤時，請先看訊息指出哪個檔案、哪一行。若包含 `unknown key`，可以對照 `schools/default.yml` 檢查欄位拼字與所在層級；若是 `YAML`解析錯誤，則先確認縮排使用空白，且冒號後保留空白，再檢查引號是否成對。

加入新設定時，請先找出檔案中是否已有相同區塊，避免在同一層重複建立 `layout:`。例如要同時修改字級與左側邊界，應把兩項設定放在同一個 `layout:` 下，並讓 `left` 再往內縮排一層：

```yaml
layout:
  font-size: 12
  margin:
    left: 3.5
```

若一次修改了多個設定，暫時無法判斷錯誤來源，可以先將剛加入的內容複製到別處保存，再移除這些設定，確認原本的文件能正常編譯。之後逐項加回，每次存檔後檢查結果，較容易找到需要修正的位置。

== 圖片或引用找不到

圖片無法載入時，請先確認檔案確實放在 `figures/` 的指定位置，再比對路徑中的檔名、副檔名與大小寫。以 `/figures/` 開頭的路徑會從專案根目錄尋找，不是從目前章節所在的資料夾開始。若曾經移動或重新命名圖片，也要一併修改章節中的路徑。

圖表引用找不到時，請檢查圖表後面是否已加上 `<標籤>`，以及名稱是否與內文的 `@標籤` 完全相同。若是文獻引用出錯，則到 `.bib` 檔案確認識別名稱，再檢查 `config.yml` 的 `bibliography.path` 是否包含該文獻檔。修正後重新編譯，確認引用已顯示為編號或文獻資料。

== PDF 沒有更新

修改內容後，請先確認終端機是否已完成這次編譯，以及是否出現錯誤。編譯失敗時，資料夾中可能仍保留上一次的 `main.pdf`，因此能開啟 PDF 並不代表新內容已經寫入。請先修正錯誤，再重新執行編譯指令。

若使用 `just watch`，請確認監看程序仍在執行，並且修改的檔案已經存檔。編譯成功後畫面仍未更新時，可以關閉再重新開啟 PDF，同時確認檢視器開啟的是目前專案中的 `main.pdf`，避免看到先前複製到其他位置的版本。
