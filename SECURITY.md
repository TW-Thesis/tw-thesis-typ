# 安全性政策

這個模板由 Typst 原始碼、YAML 設定與靜態檔案組成，編譯時不會執行外部程式，但仍可能有安全相關的問題，例如：

- 模板讀取設定時會被惡意輸入誤導，造成非預期的檔案讀取。
- 專案內附的檔案（字型、圖片、PDF）有問題。
- 相依的 Typst 套件出現已知漏洞。

## 如何回報

請使用 GitHub 的 **Security → Report a vulnerability**（私人回報），**不要**先開公開的 Issue。請說明影響、重現步驟與你使用的 Typst 版本。

## 處理方式

維護者會盡快確認回報、評估影響並修正，修正發布後再公開說明，並在你同意的情況下誌謝。

## 範圍

- 本 repo：模板與教學文件。
- 管理工具與規範更新服務位於各自的 repo（`tw-thesis-tui`、`tw-thesis-updater`），請回報到該 repo。
